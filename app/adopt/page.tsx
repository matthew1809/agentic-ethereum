import AdoptPet from '@/components/adopt-pet';

export const metadata = {
  title: 'Adopt - Agentic Ethereum',
  description: 'Find your perfect pet match with our AI matching agent.',
};

export default function AdoptPage() {
  return <AdoptPet />;
}
